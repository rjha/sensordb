
---
resource: device/list
method: GET
endpoint: http://yuktix01.cloudapp.net
description: > 
    This is a cool API
    This is a cool API
    This is a cool API
    This is a cool API
    This is a cool API

parameters:
    - name : Name
      description: >
          Name is something good
      optional : yes
    - name: age 
      description :  >
        Age is a number

sample:
    url : http://yuktix01.cloudapp.net/device/list/a001

code: |  

        require("common.inc");
        $post_url = "http://yuktix01.cloudapp.net:8080/sensordb/v1/sensor/latest" ;
        $unix_ts = time();
        $data = array("projectId" => "shail1501" , "serialNumber" => "1234", "size" => 10);

        $post_data = json_encode($data);
        $cookies = array("COOKIE1" => "ABCD1234" , "COOKIE2" => "magic1234");
        $response = get_curl_response($post_url,$post_data,$cookies,false);
        $http_code = $response["code"];
        $result = $response["data"];

        if($http_code == 200) {
            printf("success \n \t response => \n" );
            print_r($result);
            printf(" \n \n \");

        } else {
            printf("ERROR \n" );
            print_r($result);
            printf(" \n \n ");
	}
	

error:
    This is an error message 

